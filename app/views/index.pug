doctype html
html
    include head
    body
        waltz-main #{env('main.loadingMsg')}

        // IE required polyfills
        script(src='lib/es6-shim/es6-shim.min.js')
        script(src='lib/systemjs/dist/system-polyfills.js')

        script(src='lib/zone.js/dist/zone.js')
        script(src='lib/reflect-metadata/Reflect.js')
        script(src='lib/systemjs/dist/system.src.js')

        // Internationalization support
        script(src='//cdn.polyfill.io/v2/polyfill.min.js?features=Intl.~locale.nl-BE,Intl.~locale.en-US')

        script(src='lib/ng2-bootstrap/bundles/ng2-bootstrap.min.js')

        script(src='config/systemjs.config.js')
        script.
            System.import('dist')
                  .catch(function(err) {
                console.error(err);
            });